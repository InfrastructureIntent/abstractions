# 2026-09-08

## Goal

Bootstrap `InfrastructureIntent/abstractions` as the first public package-bearing contract repository and prove the repository template against a real consumer.

## Work completed

- Replaced the template solution with `InfrastructureIntent.Abstractions.sln`.
- Added the `InfrastructureIntent.Abstractions` package project.
- Added the `InfrastructureIntent.Abstractions.Tests` project.
- Applied Apache License 2.0 to the repository/package.
- Added the initial pressure-tested public contracts:
  - `IntegrationId`
  - `ResourceType`
  - `ResourceKey`
  - `ResourceIdentity`
  - `IResourceNode`
  - `IManagedResource`
  - `IExistingResource`
  - `ResourceReference<TDomain>`
- Added focused tests for identity composition, scoped keys, resource-type identity, and lifecycle-independent domain references.
- Opened and merged PR #1 to exercise Build, Test, Package, and Architecture Check workflows.
- Reviewed Copilot feedback on the identity component value types. The findings for `IntegrationId`, `ResourceType`, and `ResourceKey` were actionable and established an explicit structural contract invariant: each identity component must be non-null, non-empty, and non-whitespace.
- Updated all three public value types to enforce that invariant and added regression tests for null, empty, and whitespace values.
- Updated canonical documentation ADR-009 to record the structural invariant and explicitly distinguish it from Integration-owned naming, formatting, scoping, and canonicalization rules.
- Established milestone-driven release governance. The first milestone is `0.1.0 - Initial Abstractions`; milestones represent coherent release scope rather than schedules and do not require due dates.
- Reconstructed the work that should have existed as milestone-assigned issues:
  - #2 Bootstrap InfrastructureIntent.Abstractions repository — completed.
  - #3 Define resource identity primitives and structural invariants — completed.
  - #4 Define resource node and lifecycle contracts — completed.
  - #5 Define typed resource reference contract — completed.
  - #6 Establish validation, CI, and package baseline — completed through PR #9.
  - #7 Establish changelog, TODO, and release-note workflow — completed through PR #8.
  - #10 Finalize 0.1.0 release documentation and closeout — in progress.
- Added `CHANGELOG.md` following Keep a Changelog 1.1.0 and Semantic Versioning.
- Added `TODO.md`, explicitly keeping GitHub Issues and Milestones authoritative and requiring candidate work to be promoted into a milestone-assigned issue before implementation.
- Added `RELEASE_NOTES.md` as the human-readable release narrative and GitHub Release seed.
- Established the package-distribution policy: evolving `0.x` packages use GitHub Packages only; NuGet.org publication requires a deliberate future stability decision.
- Addressed the PR #8 Copilot finding by avoiding changelog links to release artifacts before the initial tag existed.
- Merged PR #8 after Build, Test, Package, and Architecture Check all passed and the Copilot thread was resolved.
- Set the repository/package version baseline to `0.1.0`.
- Updated the Package workflow so PRs and `main` validate package creation while version tags publish official `.nupkg` files to the InfrastructureIntent GitHub Packages NuGet feed.
- Added release-tag validation requiring a `vX.Y.Z` tag to match the package version before publication.
- Kept NuGet.org publication explicitly out of scope during the evolving `0.x` phase.
- Merged PR #9 after Build, Test, Package, and Architecture Check all passed with no outstanding Copilot findings.
- Created and pushed immutable tag `v0.1.0` from the release commit.
- Verified that `InfrastructureIntent.Abstractions 0.1.0` is available from GitHub Packages.
- Began issue #10 as a post-release documentation-only closeout; the release tag is not moved or recreated.
- Updated `CHANGELOG.md` to mark `0.1.0` released on 2026-09-08 and added valid release/compare links.
- Updated `RELEASE_NOTES.md` to Released state and recorded successful GitHub Packages publication.
- Updated `TODO.md` so `0.1.0` is a released baseline rather than an active milestone.

## Validation / evidence

- Architecture Check passed on the initial PR run.
- The first Test run failed during restore because the package project redundantly referenced `Xml2Doc.MSBuild`; the repository template already injects Xml2Doc through `Directory.Build.targets` for production projects.
- Removed the redundant project-level Xml2Doc reference so the `.csproj` remains project-specific and repository-wide defaults remain centralized.
- The first regression-test commit failed `xUnit1000` because the new test class was internal; changed the test fixture to public and re-ran CI.
- Copilot review threads for the three identity-component findings were resolved after the implementation and regression tests were added.
- PR #1 merged after the contract slice and review corrections were accepted.
- PR #8 merged after all four standard workflows passed and its single Copilot review finding was resolved.
- PR #9 merged after all four standard workflows passed with no outstanding review threads.
- `v0.1.0` successfully produced the first official `InfrastructureIntent.Abstractions 0.1.0` package in GitHub Packages.

## Architecture notes

- This repository contains public contracts only.
- Graph algorithms, reference resolution, cycle detection, compiler orchestration, and runtime behavior remain private Engine responsibilities.
- Domain semantic type and managed/existing lifecycle remain orthogonal.
- `ResourceReference<TDomain>` targets an Integration-owned domain contract, not a lifecycle-specific implementation.
- `IntegrationId`, `ResourceType`, and `ResourceKey` are structural contract values and cannot be null, empty, or whitespace.
- Infrastructure-independent structural invariants may be enforced by the public Abstractions value types themselves.
- Integration-specific naming, formatting, scoping, and canonicalization remain Integration responsibilities; enforcing non-empty structural values does not move domain validation into Abstractions.
- ADR-009 is the canonical architecture record for this identity-boundary clarification.
- No planned implementation should begin without a GitHub issue assigned to an established milestone.
- `TODO.md` is not a second backlog; GitHub Issues and Milestones remain authoritative.
- `CHANGELOG.md` records curated notable changes as work lands; `RELEASE_NOTES.md` records the final released state at closeout.
- Package validation and package publication are separate concerns: normal CI proves packages can be created; official package publication occurs only from a matching version tag.
- Release tags are immutable. Post-release documentation corrections occur on `main` and do not move published tags.

## Next

- Validate and merge issue #10 post-release documentation closeout.
- Close the `0.1.0 - Initial Abstractions` milestone after issue #10 is complete.
- Begin the next repository/milestone only after Abstractions release closeout is complete.
