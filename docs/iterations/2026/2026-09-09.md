# 2026-09-09

## Goal

Align `InfrastructureIntent.Abstractions` with `InfrastructureIntent/repository-template` `v0.1.0` under issue #12 and milestone `0.1.1 - Repository Baseline Alignment`, without changing the public contract surface released in `0.1.0`.

## Work completed

- Replaced the legacy `InfrastructureIntentEnableXml2Doc` automatic package-injection model with the finalized explicit production-project Xml2Doc intent model.
- Added an explicit `Xml2Doc.MSBuild` reference to the production Abstractions project while keeping the version centrally managed.
- Kept test projects excluded from XML/Xml2Doc documentation through `Directory.Build.targets`.
- Brought Architecture Check forward to the repository-template `v0.1.0` baseline, including SDK setup from `global.json`, required release-governance files, root-solution membership, explicit production Xml2Doc intent, test Xml2Doc exclusion, and finalized xUnit/Microsoft.Testing.Platform tooling checks.
- Aligned `AGENTS.md` and Copilot instructions with milestone-driven release and review conventions.
- Set `VersionPrefix` to `0.1.1` for the maintenance patch release.
- Updated `CHANGELOG.md`, `TODO.md`, and `RELEASE_NOTES.md` for the active milestone.

## Compatibility boundary

- No files defining public Abstractions contracts were changed.
- No new public contracts were introduced.
- No Engine behavior, graph algorithms, dependency resolution, semantic analysis, or compiler orchestration was added.
- NuGet.org publication policy remains unchanged.

## Validation pending

- Build
- Test
- Package
- Architecture Check
- Copilot/review disposition
- Confirm public API surface remains unchanged from `0.1.0`
